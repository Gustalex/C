#include <stdio.h>

int main(){

    // for loop = repeats a section of code a limited amount of times 

    for (int i =1; i <=10; i++) //i+=2(incremet by 2) i+=3(incremet by 3)...
    {
       printf("\n%d", i);
    }
    

    return 0;
}
