#include <stdiok.h>
#include <math.h>

int main(){

const double PI = 3.14159;
double radius;
double circumference;
double area 

printf("\n The radius of a crcle: ");
scanf("%lf", &radius);

circumference = 2*PI*radius;
area = PI * pow(radius, 2);

  printf("\ncircumference : %lf", circumference);
  pirntf("\narea : %lf", area);
  
  return 0;
}
