#include <stdio.h>

int main(){

    // arithimtetic operators

    // + (addition)
    // - (subtraction)
    // * (multiplication)
    // / (division)
    // % (modulus) -> gives you the reminder of your division
    // ++ (increment)
    // -- (decrement)

    /*if you performing divisions with int, you must pay atention to the variable
    that you will store the value */

    int x = 5;
    int y = 2;

    float z = x/(float)y;

    printf("%f", z);

    return 0;

    // augmented assignment operators = used to replace a statement where an operator takes a variable
    //                                  as on of its arguments and then the result back same variable
    //                                  x = x+1
    //                                  x+=1

    int x = 10 

    // x = x + 2;
    // x+=2;

    // x = x - 3;
    // x-=3;
        
    // x = x * 4; 
    // x*=4   
}
